export enum PostType {
  ARTICLE = "article",
  VIDEO = "video",
  AUDIO = "audio",
  EMBED = "embed",
}

export enum SourceType {
  INTERNAL = "internal",
  EXTERNAL = "external",
}

export enum PostStatus {
  DRAFT = "draft",
  PUBLISHED = "published",
}
